/**
 * @param {character[][]} board
 * @param {string} word
 * @return {boolean}
 */
var exist = function (board, word) {
  let arr = board
  for (let key = 0; key < word.length; key++) {

  }



}

function dfs()